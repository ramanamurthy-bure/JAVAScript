// Declaring the classes as below,
// Every class will have one special method called constructor which can be used to initialize the values
// Constructor will be called automatically whenever we create a object of the class
// If any function written within class, we should call it as method. 
class test{
    constructor(){
        console.log("I am from constructor!");
    }
}
new test();







